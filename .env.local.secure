# Secure Environment Configuration
# This file replaces .env.local with encrypted API keys

# Master password for encryption (set this via system environment)
# ENCRYPTION_MASTER_PASSWORD=your_secure_master_password_here

# Encrypted API Keys (replace the values below with encrypted versions)
OPENAI_API_KEY_ENCRYPTED=pending_encryption
GOOGLE_AI_API_KEY_ENCRYPTED=pending_encryption

# API Priority (will try Google AI after OpenAI fails)
API_PRIORITY=openai,google,huggingface,cohere,groq

# Public URLs (not sensitive)
NEXT_PUBLIC_APP_URL=http://localhost:3000/stockgpt
